<div class='grid-container'>
  {{admin-panel-header}}
  <nav id='home-nav'>
    <div class='nav-flex-container'>
      <ul>
        {{#link-to "profile" "Bongo" }}Bongo{{/link-to}}
      </ul>
      <div>
        {{#link-to "index"}}<span><strong>Medieval</strong> Lords</span>{{/link-to}}
      </div>
      <ul>
        {{sign-in-or-logout-link}}
      </ul>
    </div>
  </nav>
  {{sign-in-form}}
  {{outlet}}
</div>
